# flutter_neo_shield Example

A demo app showcasing all three flutter_neo_shield modules:

- **Log Shield** — Type any text and see PII automatically redacted
- **Clipboard Shield** — Copy sensitive data with auto-clear countdown
- **Memory Shield** — Create and manage secure string containers

## Running

```bash
cd example
flutter run
```
